import 'package:flutter/material.dart';
import 'package:get/get.dart';

class EditBookDetails extends StatelessWidget {
  const EditBookDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
